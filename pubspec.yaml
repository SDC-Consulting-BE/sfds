name: sfds
description: "Steve's Flutter Design System"
version: 0.1.0
homepage: https://github.com/SDC-Consulting-BE/sfds

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Linting
  flutter_lints: ^6.0.0
  custom_lint: ^0.7.5
  riverpod_lint: ^2.6.5
